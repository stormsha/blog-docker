version: "3"
services:

  db:
    restart: always
    image: ${MYSQL_IMAGE}
    container_name: blog_db
    volumes:
      - ./db/mysql:/var/lib/mysql
      - ./db/my.cnf:/etc/my.cnf:ro
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${BLOG_MYSQL_NAME}
    ports:
     - "3306:3306"
    networks:
      - backend

  redis:
    restart: always
    image: ${REDIS_IMAGE}
    container_name: blog_redis
    networks:
      - backend

  web:
    restart: always
    image: ${BLOG_IMAGE}
    container_name: blog_web
    environment:
      BLOG_MYSQL_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      BLOG_MYSQL_HOST: db
      BLOG_MYSQL_NAME: ${BLOG_MYSQL_NAME}
      BLOG_MYSQL_USER: root
      BLOG_MYSQL_PORT: 3306
      BLOG_REDIS_HOST: redis
      BLOG_REDIS_PORT: 6379
    env_file:
      - ./web/blog/blog.env
    command: gunicorn blog.wsgi -b 0.0.0.0:8000
    volumes:
      - ./web/blog/static:/opt/cloud/blog/static
      - ./web/blog/media:/opt/cloud/blog/media
      - ./web/blog/whoosh_index:/opt/cloud/blog/whoosh_index
    links:
      - db
      - redis
    depends_on:
      - db
      - redis
    networks:
      - backend
      - frontend

  nginx:
    restart: always
    image: ${NGINX_IMAGE}
    container_name: blog_nginx
    volumes:
      - ./web/blog/static:/opt/blog/static:ro
      - ./web/blog/media:/opt/blog/media:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./nginx/ssl_cert:/etc/nginx/ssl_cert:ro
      - ./nginx/log:/var/log/nginx
    ports:
      - "80:80"
      - "443:443"
    links:
      - web
    networks:
      - frontend

networks:
  backend:
    driver: bridge
  frontend:
    driver: bridge
